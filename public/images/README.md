# Photo Drop Folder

Drop all incoming photographer JPEGs here. Then run:

```
npm run photos
```

The script will walk through each image, suggest a category and filename, and let you confirm before copying to `Photos/organized/`.
